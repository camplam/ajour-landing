{
  "name": "ajour-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine AJOUR — landing page statique (aucune dépendance).",
  "scripts": {
    "dev": "npx serve .",
    "build": "test -f index.html && echo 'Site statique — rien à compiler.'"
  }
}
